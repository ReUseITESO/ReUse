# Secrets and Environment Variables

## Rules
- Never commit real secrets
- Every secret must have a `.example` version
- CI secrets stored in GitHub Secrets
- AWS secrets stored in AWS Secrets Manager